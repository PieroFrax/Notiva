spring:
  application:
    name: Notiva
  ai:
    openai:
      api-key: ${OPEN_AI_KEY}
      chat:
        options:
          model: ${OPEN_AI_MODEL}
          temperature: 1
          seed: 7
          logprobs: true
aws:
  defaultTextractRegion: ${AWS_DEFAULT_REGION}
