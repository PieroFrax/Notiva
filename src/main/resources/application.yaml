spring:
  application:
    name: Notiva
  ai:
    openai:
      api-key: ${OPEN_AI_KEY}
      chat:
        options:
          model: ${OPEN_AI_MODEL}
          temperature: 1
          seed: 7
aws:
  defaultTextractRegion: ${AWS_DEFAULT_REGION}
